{* $title2 - comes from xrrp hook, shows org chief for organizations or regular name for others, multilang supported *}
<div class="header-lined">
    <h1>{$title2}{if $desc} <small>{$desc}</small>{/if}</h1>
    {if $showbreadcrumb}{include file="$template/includes/breadcrumb.tpl"}{/if}
</div>
